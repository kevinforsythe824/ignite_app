import React from 'react';

import PlaceholderScreen from './PlaceholderScreen';

/** Module B — Home dashboard (placeholder). */
export const HomeScreen: React.FC = () => (
  <PlaceholderScreen
    title="Home"
    description="Dashboard, recent decks, and tournament preview will live here."
  />
);

export default HomeScreen;
