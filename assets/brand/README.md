# Ignite brand assets

## Production flame

- `ignite-flame.png` — exact Flame Study artwork (transparent alpha already present in source; copied without altering flame pixels).

Wired via:

- `src/features/auth/components/igniteFlameAsset.ts`
- `expo-splash-screen` `image` in `app.json`
