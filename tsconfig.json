{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "types": ["jest"]
  },
  "exclude": [
    "${configDir}/node_modules",
    "${configDir}/babel.config.js",
    "${configDir}/metro.config.js",
    "${configDir}/jest.config.js",
    "${configDir}/jest.firestore-rules.config.js",
    "${configDir}/android",
    "${configDir}/ios",
    "scripts",
    "__tests__/firestore-rules"
  ]
}
