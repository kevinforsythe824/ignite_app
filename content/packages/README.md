# Generated content packages

Derived artifacts. Edit the authoring workbooks and regenerate — do not treat these JSON files as source.

Phase 2A.1 writes `content.json`, `manifest.json`, and `validation-report.json` under `{seasonId}/`. Nothing here is imported to Firebase yet (Phase 2A.2).
